spring:

  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/outbox_pattern
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    jdbc:
      dialect: mysql

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


  application:
    name: OrderService
